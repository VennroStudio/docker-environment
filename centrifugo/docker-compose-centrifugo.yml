name: ${PROJECT_NAME}

services:
  centrifugo:
    image: centrifugo/centrifugo:latest
    container_name: centrifugo-container
    restart: unless-stopped
    command: centrifugo -c config.yaml
    ports:
      - "127.0.0.1:${CENTRIFUGO_PORT}:9000"
    volumes:
      - ./config.yaml:/centrifugo/config.yaml:ro
    environment:
      CENTRIFUGO_CLIENT_TOKEN_HMAC_SECRET_KEY: ${CENTRIFUGO_HMAC_SECRET_KEY}
      CENTRIFUGO_CLIENT_ALLOWED_ORIGINS: ${CENTRIFUGO_ALLOWED_ORIGINS}
      CENTRIFUGO_ADMIN_ENABLED: "true"
      CENTRIFUGO_ADMIN_PASSWORD: ${CENTRIFUGO_ADMIN_PASSWORD}
      CENTRIFUGO_ADMIN_SECRET: ${CENTRIFUGO_ADMIN_SECRET}
      CENTRIFUGO_HTTP_API_KEY: ${CENTRIFUGO_HTTP_API_KEY}
      CENTRIFUGO_HTTP_SERVER_INTERNAL_PORT: "9000"
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
    networks:
      - network-name

networks:
  network-name:
    name: ${NETWORK}
    external: true
