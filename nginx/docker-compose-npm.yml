name: vennro

services:
  nginx-proxy-manager:
    image: 'jc21/nginx-proxy-manager:latest'
    container_name: nginx-container
    restart: unless-stopped
    ports:
      - '80:80'
      - '81:81'
      - '443:443'
    volumes:
      - ./docker/server/nginx/data:/data
      - ./docker/server/nginx/letsencrypt:/etc/letsencrypt
    networks:
      - proxy